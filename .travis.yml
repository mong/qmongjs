sudo: false

cache:
  npm: false

language: node_js

node_js:
  - 12


after_success:
  - ./node_modules/codecov/bin/codecov -e TRAVIS_NODE_VERSION -f coverage/*.json codecov --disable=gcov
  - ./node_modules/codecov/bin/codecov -e TRAVIS_NODE_VERSION codecov --disable=gcov